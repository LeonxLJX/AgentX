# AgentX Pitching Scripts — How to Bid Each Module

> Usage: when you see a matching job on a platform (Upwork / Fiverr /
> Freelancer / domestic marketplaces), open with the one-liner + delivery
> promise below, and attach the module demo link / screenshot.

---

## 1. TextClassifier — Text Classification

**Matching jobs**: comment sentiment classification, email auto-routing,
ticket intent recognition, document type tagging.

> "I have a ready EN/ZH text-classification module (TF-IDF + logistic
> regression/SVM) with multi-class support, confidence and full probability
> output, shippable as a single model artifact. A 4-intent support-ticket demo
> is included; give me a few hundred labeled samples and it can go live within
> a week."

---

## 2. PhaseDetect — NLP Phase Detection

**Matching jobs**: sentiment analysis, topic clustering, keyword/entity
extraction, social-listening preprocessing.

> "Deliver a lightweight NLP detector: sentiment (EN/ZH), topic clustering
> (TF-IDF + KMeans) and entity extraction (email/phone/date/money) in one
> analysis call - no heavy dependencies, fully offline-capable."

---

## 3. Calibrator — Probability Calibration

**Matching jobs**: model probabilities are over-confident, credit/risk model
calibration, model evaluation reports.

> "I fix over-confident models: recalibrate your probability output with
> Platt/Isotonic and deliver a before/after Brier & log-loss comparison plus a
> reliability diagram. Small change, measurable gain."

---

## 4. Scheduler — Job Scheduling

**Matching jobs**: task scheduling tools, automation pipeline dispatch,
resource rostering, project Gantt charts.

> "A ready scheduling engine: priorities (WSPT), dependency graphs (DAG) and
> parallel-machine scheduling, outputting Gantt-ready data behind a REST API.
> Objective functions are customizable (makespan / weighted completion)."

---

## 5. Optimizer — Path Optimization

**Matching jobs**: delivery route planning, courier/food-delivery routing,
multi-vehicle dispatch (VRP), loading optimization (knapsack).

> "A path-optimization trio: TSP single routes (nearest-neighbour + 2-opt),
> capacity-constrained multi-vehicle routing (Clarke-Wright savings) and load
> optimization (0/1 knapsack DP) - deterministic solvers behind an API, with
> route visualization in the frontend."

---

## 6. Geometry — Computational Geometry

**Matching jobs**: convex hull, collision detection (games/simulation), image
processing (resize/blur/edges).

> "Computational geometry + image processing: convex hull (area/perimeter),
> polygon collision with minimum translation vector (collision response
> ready), and a chainable image pipeline (gray/resize/blur/edge detection)
> with optional OpenCV acceleration."

---

## 7. ETLEngine — Data Cleaning & OCR

**Matching jobs**: Excel/CSV cleaning and dedupe, paper-document OCR to
structured data, messy-data governance.

> "A data-cleaning pipeline: automatic dedupe, missing-value fill, type
> inference and outlier handling with a before/after audit report; OCR can be
> layered on to turn scans and images into structured tables. Enterprise
> messy-data problems solved end to end."

---

## 8. FinanceModels — Financial Modeling

**Matching jobs**: time-series forecasting, factor mining, quant feature
engineering, risk-scoring support.

> "A financial time-series toolbox: SMA/EMA/volatility/momentum/RSI/Bollinger/
> Sharpe/max-drawdown factors, plus linear/Ridge/AR forecasts with rolling
> backtest (RMSE/MAE/MAPE). Closed-form, auditable - suited to strategy
> research and small forecasting systems."

---

## 9. REST API + Frontend — Full-Stack Delivery

**Matching jobs**: wrap algorithms as APIs, deliver a demo-able system, Docker
deployment.

> "All modules are already wrapped as FastAPI endpoints (auto Swagger docs)
> with a single-page visualized frontend and one-command Docker deployment.
> Tell me the requirement and you get a complete system that runs, demos and
> integrates."

---

## Combined Pitch (Whole Kit)

> "AgentX is a nine-module full-stack AI outsourcing toolkit: text
> classification, NLP detection, probability calibration, job scheduling,
> path optimization, computational geometry, data cleaning/OCR, financial
> modeling, plus a REST API and visualized frontend. Whatever the requirement,
> I have a ready module - fastest path from job post to demo and delivery."

## Reference Pricing

| Module | Reference price |
|--------|-----------------|
| Text classification / OCR / data cleaning | $50–600 |
| Probability calibration / model evaluation | $300–800 |
| Scheduling / path optimization | $500–3000 |
| Computational geometry / image processing | $200–800 |
| Financial modeling | $500–2000 |
| Full stack (API + frontend + Docker) | $800–5000 |
